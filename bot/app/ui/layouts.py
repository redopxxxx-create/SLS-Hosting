def card(title: str, body: str) -> str:
    return (
        f"╭━━━〔 {title} 〕━━━╮\n\n"
        f"{body}\n\n"
        "╰━━━━━━━━━━━━━━━━━━╯"
    )


def loading(step: str) -> str:
    return f"⟡ {step}\n━━━━━━━━━━━━━━━━━━"
